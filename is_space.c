#include "headerfile.h"

/**
 * is_space - returns 1 if argument is space, 0 otherwise
 * @arg: argument
 * Return: 0 or 1
 */
int is_space (char arg)
{
	if (arg == ' ')
		return (1);
	else
		return (0);
}
